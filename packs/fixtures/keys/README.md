# Test-Only Fixture Keys

These Ed25519 keys are for local fixture pack smoke tests only.

- `test-ed25519-private-key.hex`: deterministic private key seed used by `make market-data-pack-fixture`
- `test-ed25519-public-key.hex`: matching public key

Never use these keys for production signing or release assets.
